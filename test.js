var DB = require('./db.js');

var db = new DB();


var track = {
    trackId: 58333845,
    playPathAacv224: 'url',
    title: 'example',
    duration: 256,
    createdAt: 113355666,
    test: 'xxx'
}

// db.deleteAll(1);
